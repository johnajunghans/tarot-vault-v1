import { describe, it } from 'vitest'

describe('SpreadCanvas', () => {
    it.todo('shows the empty prompt only when there are no cards and the canvas is editable')
    it.todo('renders the marquee overlay only while marquee selection is active')
    it.todo('shows the offscreen pointer overlay only when derived pointers exist')
})
